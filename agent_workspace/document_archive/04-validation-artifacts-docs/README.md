# Validation Artifacts

本目录用于存放架构阶段的临时验证工程和脚手架自测产物。

当前内容：

- `scaffold_test_twins`：用于验证 `TwinsSimulationAgent` 脚手架输出是否可正确落盘
- `scaffold_test_central`：用于验证 `CentralSchedulingAgent` 脚手架输出是否可正确落盘

说明：

- 这些目录不是正式交付物
- 正式对外交付内容位于 `execution_agent/outputs`
- 如需清理历史验证工程，可优先从本目录处理
