param(
    [Parameter(Mandatory = $true)]
    [string]$ProjectName,

    [Parameter(Mandatory = $true)]
    [string]$OutputDir,

    [string]$AgentClass = 'DemoTwinsAgent',
    [string]$AgentCode = 'DEMO_TWINS_AGENT',
    [string]$AgentType = 'TWINS_SIMULATION_AGENT',
    [ValidateSet('TwinsSimulationAgent', 'OntologySimulationAgent', 'CentralSchedulingAgent', 'ModelCalculationAgent', 'OutflowPlanAgent')]
    [string]$BaseClass = 'TwinsSimulationAgent'
)

$projectRoot = Join-Path $OutputDir $ProjectName
$agentAppDir = Join-Path $projectRoot 'agent_app'
$confDir = Join-Path $projectRoot 'conf'

New-Item -ItemType Directory -Force -Path $projectRoot | Out-Null
New-Item -ItemType Directory -Force -Path $agentAppDir | Out-Null
New-Item -ItemType Directory -Force -Path $confDir | Out-Null

$pyproject = @"
[build-system]
requires = ["setuptools>=42", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "$ProjectName"
version = "0.1.0"
description = "External Hydros agent project generated by scaffold"
readme = "README.md"
requires-python = ">=3.9"
dependencies = [
    "hydros-agent-sdk",
]
"@

$readme = @"
# $ProjectName

这是一个基于 `hydros-agent-sdk` 自动生成的外部 Agent 项目。

## 启动步骤

1. 安装依赖

```powershell
pip install -e .
在 `hydros-python-sdk` 项目根目录执行 `pip install -e .`
```

2. 修改配置

- `conf/env.properties`
- `conf/agent.properties`

3. 启动

```powershell
python launcher.py
```
"@

$envProps = @"
mqtt_broker_url=tcp://127.0.0.1
mqtt_broker_port=1883
mqtt_topic=/hydros/commands/coordination/default_cluster
hydros_cluster_id=default_cluster
hydros_node_id=external_node_001
mqtt_username=
mqtt_password=
"@

$agentProps = @"
agent_code=$AgentCode
agent_type=$AgentType
agent_name=$AgentClass
"@

$initPy = @"
"""$ProjectName agent package."""
"@

$businessEngine = @"
class DemoBusinessEngine:
    \"\"\"最小业务引擎示例。\"\"\"

    def initialize(self, context):
        self.context = context

    def run(self, step: int, payload=None):
        return {
            'step': step,
            'status': 'ok',
            'payload': payload or {},
            'biz_scene_instance_id': getattr(self.context, 'biz_scene_instance_id', ''),
        }
"@

switch ($BaseClass) {
    'TwinsSimulationAgent' {
        $agentImpl = @"
from hydros_agent_sdk import $BaseClass

from agent_app.business_engine import DemoBusinessEngine


class $AgentClass($BaseClass):
    def _initialize_twins_model(self):
        self.engine = DemoBusinessEngine()
        self.engine.initialize(self.context)

    def _execute_twins_simulation(self, step: int):
        self.engine.run(step)
        return []
"@
    }
    'OntologySimulationAgent' {
        $agentImpl = @"
from hydros_agent_sdk import $BaseClass

from agent_app.business_engine import DemoBusinessEngine


class $AgentClass($BaseClass):
    def _initialize_ontology_model(self):
        self.engine = DemoBusinessEngine()
        self.engine.initialize(self.context)

    def _execute_ontology_simulation(self, step: int):
        self.engine.run(step)
        return []
"@
    }
    'CentralSchedulingAgent' {
        $agentImpl = @"
from hydros_agent_sdk import $BaseClass
from hydros_agent_sdk.protocol.commands import SimTaskInitRequest, SimTaskInitResponse, SimTaskTerminateRequest, SimTaskTerminateResponse
from hydros_agent_sdk.protocol.models import CommandStatus, AgentBizStatus

from agent_app.business_engine import DemoBusinessEngine


class $AgentClass($BaseClass):
    def on_init(self, request: SimTaskInitRequest) -> SimTaskInitResponse:
        self.load_agent_configuration(request)
        self.engine = DemoBusinessEngine()
        self.engine.initialize(self.context)
        self.state_manager.init_task(self.context, [self])
        self.state_manager.add_local_agent(self)
        object.__setattr__(self, 'agent_biz_status', AgentBizStatus.ACTIVE)
        return SimTaskInitResponse(
            context=self.context,
            command_id=request.command_id,
            command_status=CommandStatus.SUCCEED,
            source_agent_instance=self,
            created_agent_instances=[self],
            managed_top_objects={},
            broadcast=False,
        )

    def on_optimization(self, step: int):
        self.engine.run(step)
        return []

    def on_terminate(self, request: SimTaskTerminateRequest) -> SimTaskTerminateResponse:
        self.state_manager.terminate_task(self.context)
        self.state_manager.remove_local_agent(self)
        return SimTaskTerminateResponse(
            context=self.context,
            command_id=request.command_id,
            command_status=CommandStatus.SUCCEED,
            source_agent_instance=self,
            broadcast=False,
        )
"@
    }
    'ModelCalculationAgent' {
        $agentImpl = @"
from hydros_agent_sdk import $BaseClass
from hydros_agent_sdk.protocol.commands import SimTaskInitRequest, SimTaskInitResponse, SimTaskTerminateRequest, SimTaskTerminateResponse
from hydros_agent_sdk.protocol.models import CommandStatus, AgentBizStatus

from agent_app.business_engine import DemoBusinessEngine


class $AgentClass($BaseClass):
    def on_init(self, request: SimTaskInitRequest) -> SimTaskInitResponse:
        self.load_agent_configuration(request)
        self.engine = DemoBusinessEngine()
        self.engine.initialize(self.context)
        self.state_manager.init_task(self.context, [self])
        self.state_manager.add_local_agent(self)
        object.__setattr__(self, 'agent_biz_status', AgentBizStatus.ACTIVE)
        return SimTaskInitResponse(
            context=self.context,
            command_id=request.command_id,
            command_status=CommandStatus.SUCCEED,
            source_agent_instance=self,
            created_agent_instances=[self],
            managed_top_objects={},
            broadcast=False,
        )

    def on_model_calculation(self, hydro_event):
        self.engine.run(0, {'event': str(hydro_event)})
        return []

    def on_terminate(self, request: SimTaskTerminateRequest) -> SimTaskTerminateResponse:
        self.state_manager.terminate_task(self.context)
        self.state_manager.remove_local_agent(self)
        return SimTaskTerminateResponse(
            context=self.context,
            command_id=request.command_id,
            command_status=CommandStatus.SUCCEED,
            source_agent_instance=self,
            broadcast=False,
        )
"@
    }
    'OutflowPlanAgent' {
        $agentImpl = @"
from hydros_agent_sdk import $BaseClass
from hydros_agent_sdk.protocol.commands import SimTaskTerminateRequest, SimTaskTerminateResponse
from hydros_agent_sdk.protocol.models import CommandStatus

from agent_app.business_engine import DemoBusinessEngine


class $AgentClass($BaseClass):
    def on_init(self, request):
        response = super().on_init(request)
        self.engine = DemoBusinessEngine()
        self.engine.initialize(self.context)
        return response

    def on_outflow_time_series(self, request):
        self.engine.run(0, {'request': request.command_id})

    def on_terminate(self, request: SimTaskTerminateRequest) -> SimTaskTerminateResponse:
        self.state_manager.terminate_task(self.context)
        self.state_manager.remove_local_agent(self)
        return SimTaskTerminateResponse(
            context=self.context,
            command_id=request.command_id,
            command_status=CommandStatus.SUCCEED,
            source_agent_instance=self,
            broadcast=False,
        )
"@
    }
}

$launcher = @"
from hydros_agent_sdk import (
    SimCoordinationClient,
    HydroAgentFactory,
    MultiAgentCallback,
    load_env_config,
)

from agent_app.agent_impl import $AgentClass


def main():
    env_config = load_env_config('./conf/env.properties')

    factory = HydroAgentFactory(
        agent_class=$AgentClass,
        config_file='./conf/agent.properties',
        env_config=env_config,
    )

    callback = MultiAgentCallback(node_id=env_config['hydros_node_id'])
    callback.register_agent_factory('$AgentCode', factory)

    client = SimCoordinationClient(
        broker_url=env_config['mqtt_broker_url'],
        broker_port=int(env_config['mqtt_broker_port']),
        topic=env_config['mqtt_topic'],
        sim_coordination_callback=callback,
        mqtt_username=env_config.get('mqtt_username'),
        mqtt_password=env_config.get('mqtt_password'),
    )

    callback.set_client(client)
    client.start()

    try:
        import time
        while True:
            time.sleep(1)
    except KeyboardInterrupt:
        client.stop()


if __name__ == '__main__':
    main()
"@

Set-Content -Path (Join-Path $projectRoot 'pyproject.toml') -Value $pyproject -Encoding UTF8
Set-Content -Path (Join-Path $projectRoot 'README.md') -Value $readme -Encoding UTF8
Set-Content -Path (Join-Path $confDir 'env.properties') -Value $envProps -Encoding UTF8
Set-Content -Path (Join-Path $confDir 'agent.properties') -Value $agentProps -Encoding UTF8
Set-Content -Path (Join-Path $agentAppDir '__init__.py') -Value $initPy -Encoding UTF8
Set-Content -Path (Join-Path $agentAppDir 'business_engine.py') -Value $businessEngine -Encoding UTF8
Set-Content -Path (Join-Path $agentAppDir 'agent_impl.py') -Value $agentImpl -Encoding UTF8
Set-Content -Path (Join-Path $projectRoot 'launcher.py') -Value $launcher -Encoding UTF8

$summary = @"
Hydros 外部 Agent 项目已生成。

Project Root: $projectRoot
Agent Class : $AgentClass
Agent Code  : $AgentCode
Agent Type  : $AgentType
Base Class  : $BaseClass

下一步：
1. 在 `hydros-python-sdk` 项目根目录执行 `pip install -e .`
2. 修改 conf/env.properties
3. 修改 conf/agent.properties
4. python launcher.py
"@

Write-Host $summary

