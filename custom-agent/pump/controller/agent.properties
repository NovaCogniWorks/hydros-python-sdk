# Hydro Agent Configuration
# Pump Controller Agent

# Agent identification
agent_code=PUMP_CONTROLLER_AGENT
agent_type=CONTROLLER_AGENT
agent_name=Pump Station Local Controller

# Agent meta
version=0.1.0
description=Pump station local controller that receives blade-angle/on-off commands and reports unit status

# Agent drive mode
drive_mode=SIM_TICK_DRIVEN

# Managed pump units (comma-separated object IDs, or JSON list)
pump_units=[]

# Per-unit rated parameters (example: unit.1001.rated_flow=10.0)
# unit.<id>.rated_flow=1.0
# unit.<id>.rated_power=1.0
